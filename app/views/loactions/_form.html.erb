<%= form_with(model: loaction) do |form| %>
  <% if loaction.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(loaction.errors.count, "error") %> prohibited this loaction from being saved:</h2>

      <ul>
        <% loaction.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
